{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is an example project of backend in expressJS having all the authentication functionality including email verification, access and refresh tokens.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && cd ../frontend && npm install && npm install path @vitejs/plugin-react vite && npm i --save-dev @types/path && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Moeez Ali",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.0",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.14"
  }
}
